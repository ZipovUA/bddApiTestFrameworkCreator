/*
    here should be list of connected default api body classes
    const SomeEndpointBody = require('./endpoint');
    const SomeAnotherEndpointBody = require('./anotherEndpoint');
*/

const DefaultApiBody = {    
        /*
            here should be list of properties
            that way to connected default api body classes
            someEndpointBody : SomeEndpointBody
            someAnotherEndpointBody: SomeAnotherEndpointBody
        */        
}

module.exports = DefaultApiBody;